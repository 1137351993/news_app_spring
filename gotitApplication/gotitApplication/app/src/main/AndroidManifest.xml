<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gotitapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GotItApplication"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity android:name=".login.signup"></activity>
        <activity android:name=".ContentActivity"></activity>
<!--        <activity android:name=""></activity>-->MainActivity2
        <activity android:name=".MainActivity2"></activity>
        <activity android:name=".users.PersonInfo"></activity>
        <activity android:name=".news_list"></activity>
        <activity android:name=".attention.attention"></activity>
        <activity
            android:name=".login.login"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
    <uses-permission android:name="android.permission.INTERNET"/>

</manifest>